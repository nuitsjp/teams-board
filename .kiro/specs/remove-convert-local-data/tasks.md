# Implementation Plan

- [x] 1. ローカル変換パイプラインのコード・テスト・データを削除する
- [x] 1.1 (P) エントリポイントスクリプトとビジネスロジックモジュールを削除する
  - ローカル変換の実行スクリプトを削除する
  - ローカル変換パイプラインを構成する全9モジュール（コマンド、アダプタ、バリデータ、リポーター、リーダー、ライター、集約サービス）をディレクトリごと削除する
  - _Requirements: 1.1, 2.1, 2.2_

- [x] 1.2 (P) ローカル変換パイプラインのテストを削除する
  - ローカルバッチモジュールの単体テスト（9ファイル）をディレクトリごと削除する
  - ローカル変換パイプラインの統合テストをディレクトリごと削除する（唯一のファイルのため）
  - _Requirements: 3.1, 3.2_

- [x] 1.3 (P) ローカル変換用のサンプル CSV データを削除する
  - ローカル変換の入力として使用されていた全サンプル CSV（13ファイル）をディレクトリごと削除する
  - _Requirements: 4.1_

- [x] 2. package.json からローカル変換スクリプトのエントリを削除する
  - `convert:local-data` スクリプトエントリを削除する
  - 他のスクリプト（dev, build, test 等）に影響がないことを確認する
  - _Requirements: 1.2, 5.1, 5.2_

- [x] 3. アーキテクチャドキュメントからローカル変換関連の記述を削除する
  - 目的と前提セクションからローカル変換フローの説明を除去する
  - 稼働境界セクションからローカル変換バッチの記述を除去する
  - 全体アーキテクチャ図からローカル変換サブグラフとサンプルデータノード、関連エッジを除去する
  - ローカル変換フローのシーケンス図セクションを丸ごと削除する
  - 運用支援スクリプト一覧からローカル変換スクリプトの行を除去する
  - テスト構成セクションからローカルバッチテストの記述を除去する
  - ディレクトリ要約からローカルバッチ、サンプルデータ、変換スクリプトの行を除去する
  - _Requirements: 6.1, 6.2, 6.3_

- [x] 4. 削除後のプロジェクト整合性を検証する
  - 全テストスイートを実行し、すべてのテストがパスすることを確認する
  - lint を実行し、新たなエラーが発生していないことを確認する
  - リポジトリ内に `local-batch` モジュールへの import/require 参照が残っていないことを検索で確認する（スペックドキュメントを除く）
  - _Requirements: 3.3, 7.1, 7.2, 7.3_
