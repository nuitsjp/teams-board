# Implementation Plan

- [x] 1. Deploy-StaticFiles.ps1 に data/ ディレクトリ除外フィルタを追加する
  - ファイル一覧取得処理にパイプラインフィルタを追加し、相対パスが `data/` で始まるファイルをアップロード対象から除外する
  - 除外フィルタは既存の相対パス計算ロジック（Blob名生成と同一手法）を再利用する
  - フィルタ後のファイル数カウントを配列化して正確に取得する（パイプライン結果が単一要素の場合の対策）
  - スクリプトの `.DESCRIPTION` セクションに data/ 除外の旨を追記する
  - `public/` 配下の非 data/ ファイル（HTML、CSS、JS、ライブラリ）が従来通りアップロードされることを確認する
  - `src/` パスのデプロイに副作用がないことを確認する
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
